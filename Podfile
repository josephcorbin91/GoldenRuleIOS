# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Golden Rule' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod ‘Firebase/Core’ 
  pod ‘Firebase/Auth’ 
  pod 'Firebase/Messaging'
  pod ‘Player’, ‘~> 0.8.0’
  pod 'Fabric' 
  pod 'Crashlytics'
  pod ‘FirebaseUI’ 


  # Pods for GoldenRuleIOS

  target 'Golden RuleTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Golden RuleUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
